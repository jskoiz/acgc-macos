# Exact-PID host-runner cleanup replay — umbrella `714958f46` — 2026-08-24

## Outcome

This local umbrella-only candidate replays the independently reviewed exact-PID
cleanup implementation onto the current live umbrella `origin/main` at
`714958f466ef107ac3b6f64165fe86e1ca8f6d04`. It owns the two runner paths plus
this evidence file and the synchronized README and lane-board entries. The
candidate is local and unpushed; independent immutable review and any hosted
PR/merge remain separate gates.

The runner cleanup contract is narrow and fail-closed:

- It records one launched host PID with its macOS `lstart` start identity and
  exact command, under an atomic lock and atomic record publication.
- It classifies the recorded PID before cleanup and refuses malformed, stale,
  zombie, unknown, or mismatched state without signaling an unowned process.
- It revalidates the live state, start identity, and command immediately before
  `TERM`, and again immediately before `KILL` after the bounded grace probes.
- Lock contention, record publication failure, and ownership uncertainty do not
  fall back to `pgrep`, `pkill`, `killall`, or `kill -f`. A same-shell `$!`
  child is contained and reaped only where the launch transaction owns it.

This proves a shell/fake-callback ownership contract only. It does not prove a
real process launch or cleanup, AppKit/LaunchServices behavior, LLDB behavior,
Metal/device behavior, assets, hosted checks, or playability.

## Exact revisions and scope

- Replay base: umbrella `origin/main`
  `714958f466ef107ac3b6f64165fe86e1ca8f6d04`.
- Replay branch: `c1/lane-exact-pid-cleanup-714`.
- Fresh replay worktree: `/private/tmp/acgc-lane-exact-pid-replay-714.mzJQXi`.
- Reviewed predecessor base: `94cdee0b3e9f6badeff1871fdc27b07a18d21520`.
- Reviewed predecessor tip: `cfaf226caeeee139cb210a9f3c2bc5ca58276f96`.
- Current PC snapshot crosswalk: `586cf7a616cd38149c911bd4bc8fb2f1de638de4`.
- Decomp oracle: `09ca8e8b5b24e6ab44047ee980cf0088ad7ecb4c`.

The source replay owns exactly:

- `script/build_and_run.sh`
- `scripts/probes/test_build_and_run_process_ownership.sh`

The umbrella-only synchronization owns exactly:

- `README.md`
- `docs/LANE-BOARD.md`
- `docs/evidence/EXACT-PID-CLEANUP-714958F46-2026-08-24.md`

No upstream gitlink, PC source, decomp source, canonical verification runner,
workflow, CMake file, asset, or build root was changed or created.

The two runner files are byte-for-byte equivalent to the reviewed source patch
`94cdee0b3..cfaf226ca` when applied to the unchanged two paths at umbrella
base `714958f46`.

## Source and oracle crosswalk

The umbrella implementation is in `script/build_and_run.sh`; the deterministic
fixture interposes the ownership probes and signal callbacks in
`scripts/probes/test_build_and_run_process_ownership.sh`. The existing PC
macOS target is a `MACOSX_BUNDLE` in
`upstream/ACGC-PC-Port/pc/apple/CMakeLists.txt`, and
`upstream/ACGC-PC-Port/pc/apple/src/main.m` parses ordinary argv before
starting `NSApplication`. That static crosswalk supports the direct inner-bundle
launch shape; no real AppKit launch was performed here and no resource or
LaunchServices claim follows. The decomp oracle has no process-supervisor or
host-runner counterpart, so there is no decomp implementation to port for this
contract.

## Bounded verification

All commands below ran from the fresh replay worktree and were serialized. The
result was zero exit status unless stated otherwise.

| Gate | Command/result | Boundary |
| --- | --- | --- |
| Shell syntax | `bash -n script/build_and_run.sh scripts/probes/test_build_and_run_process_ownership.sh` — PASS | Parses the two owned scripts only. |
| Zsh syntax | `zsh -n script/build_and_run.sh scripts/probes/test_build_and_run_process_ownership.sh` — PASS | Parses the two owned scripts only. |
| Runner help | `bash script/build_and_run.sh --help` — PASS, exit `0` | Exercises argument/help dispatch; no launch. |
| Ownership fixture | `bash scripts/probes/test_build_and_run_process_ownership.sh` — PASS | Covers atomic lock, exact record, lstart separator parsing, live/stale/zombie/unknown state, malformed/extra-line records, identity reuse before `TERM` and `KILL`, bounded fallback, direct `contain_unrecorded_host` record-publication-failure containment, and idempotence through fake callbacks. |
| Forbidden broad matching | Scoped `rg` for `pgrep`, `pkill`, `killall`, and `kill -f` in both owned scripts — no matches | Static absence only; no process was signaled. |
| Source replay equivalence | Diff of replayed two paths against reviewed `94cdee0b3..cfaf226ca` patch — PASS | Confirms the reviewed source change was replayed without source drift. |
| Whitespace | `git diff --check` — PASS | Candidate diff has no whitespace errors. |

The fixture’s host-shaped `ps` case uses the actual macOS five-space separator
after the 24-byte `lstart` field and verifies the command comparison is exact.
The fake PID-reuse cases flip identity between the initial query and the
last-moment signal checks; no signal is emitted after a mismatch. The lock and
record-failure cases verify no unconditional cleanup signal is attempted. The
fixture directly tests `contain_unrecorded_host` for record-publication-failure
containment. Source inspection confirms `launch_owned_host` routes identity or
record-publication failure to that helper; the fixture does not force
`write_owned_host_record` or `launch_owned_host` itself to fail at those actual
call sites.

No full link, CMake/CTest, runtime process, LLDB session, real signal, asset
access, hosted check, or downloaded artifact was used or claimed.

## Finding and successor

Within this bounded source/fixture gate, no P0 or P1 was observed. The retained
P2 boundary is that real macOS launch/cleanup and operator-owned LLDB behavior
remain unexecuted by contract; this candidate must not be described as runtime
process proof. A successor independent immutable review should verify the exact
five-path scope and current-base merge-tree before the integration owner performs
live authentication/workflow checks and any push, PR, or merge.
