# PC raw TEV/KONST shadow at `037689462`

Date: 2026-08-14

Canonical PC commit: `037689462eaa08b1f08c24748276a0c82bf169c5`

Remote worker: `c1/lane-tev-raw-shadow-m3` at `11cf5db0062dddf7a644e995a8c3fae5e0590516`

Worker base: `216d1e24be5fbb85a3a394cdb1bfd50545b6b6f4`

Decomp oracle: `09ca8e8b5b24e6ab44047ee980cf0088ad7ecb4c`

## Result

The reviewed worker commit is integrated on `c1/macos-host-launch` as
`037689462`. It adds setter-owned raw provenance beside the existing normalized
float TEV state:

- four PREV/REG0-2 raw records;
- four K0-3 raw KONST records;
- exact widened components;
- separate validity and source fields; and
- an explicit malformed state for out-of-range signed S10 input.

Reset leaves every raw record unavailable. `GXSetTevColor`,
`GXSetTevColorS10`, and `GXSetTevKColor` record their logical input before the
existing normalized-float equality early return, so setter overwrite order is
preserved even when the host float value does not change. Invalid IDs remain
no-ops. Malformed S10 input cannot masquerade as a valid or merely unavailable
canonical value. The existing float path and renderer behavior are unchanged.

## Two-upstream crosswalk

- PC state and setters: `pc/include/pc_gx_internal.h` and
  `pc/src/pc_gx.c`.
- Decomp bounds and semantics: `src/static/dolphin/gx/GXTev.c`, public GX TEV
  enums, initialization, emu64, J2D/J3D, and Famicom callers.
- The decomp contract has four TEV registers, four KONST colors, and signed
  S10 limits `-1024..1023`. The new PC shadow retains those values rather than
  attempting to reconstruct them from normalized floats.

## Exact source delta

- `pc/include/pc_gx_internal.h`
- `pc/src/pc_gx.c`
- `pc/tests/pc_gx_tev_raw_shadow_fixture.c`
- `pc/CMakeLists.txt`

No shader, canonical ABI, legacy V1-V4 packet, Apple/Metal, or decomp source
changed.

## Verification

Remote M3 Max verification:

- native focused CTest: `1/1` passed;
- combined ASan/UBSan focused CTest: `1/1` passed with no diagnostics;
- changed production object compiled;
- pre- and post-commit `git diff --check` passed; and
- the bounded Windows probe was blocked by the missing MinGW/ILP32
  toolchain/sysroot, so there is no Windows sign-off.

Exact integrated local verification used fresh roots:

```sh
cmake -S upstream/ACGC-PC-Port/pc \
  -B /private/tmp/acgc-integrate-tev-shadow-037689-native \
  -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON \
  -DPC_DARWIN_COMPILE_AUDIT=ON \
  -DCMAKE_C_COMPILER=/usr/bin/clang -DCMAKE_CXX_COMPILER=/usr/bin/clang++
cmake --build /private/tmp/acgc-integrate-tev-shadow-037689-native \
  --target acgc_pc_gx_tev_raw_shadow_fixture --parallel 1
ctest --test-dir /private/tmp/acgc-integrate-tev-shadow-037689-native \
  -R '^acgc_pc_gx_tev_raw_shadow_fixture$' --output-on-failure --parallel 1
```

Native passes `1/1`. The same focused test passes `1/1` under combined
ASan/UBSan with `ASAN_OPTIONS=detect_leaks=0` and
`UBSAN_OPTIONS=halt_on_error=1`; no sanitizer diagnostic was emitted. Existing
AppleClang warnings were the decomp `INT_MIN` redefinition and unsupported
`-Wno-builtin-declaration-mismatch`. Leak detection was disabled, so this is
not leak-free proof.

## Evidence boundary

This proves only setter-owned PC CPU raw TEV/KONST provenance and preservation
of the existing float path. It does **not** implement the 2,560-byte canonical
TEV section, a live canonical snapshot, callback, renderer, Metal
encode/present/readback, pixel, input, audio, save/reload, device, iOS, Windows
runtime, or playability. Lane 168 independently owns the portable canonical TEV
ABI; a later producer lane must join the two reviewed contracts fail-closed.
